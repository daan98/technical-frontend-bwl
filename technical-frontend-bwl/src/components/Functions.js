/* Por razones de tiempo (no tardar más de 5 días en realizar la app) este archivo queda vacio.
 * Se pueden colocar las funciones utilizadas en cada compoenente y así tenerlos con menos código para mejor lectura 

 * EJEMPLO:

 * FUNCIÓN DE Login.js
 * 
 * Aqui en Functions.js se colocaría lo siguiente:
 * 
 ** export const onSubmitForm = (parametros que usa la función separados por coma) => {  CÓDIGO DE LA FUNCIÓN  };
 *
 * Ya en Login.js se tendría que colocar en la sección de importes:
 * 
 ** import { onSubmitForm } from './Functions.js';
 * 
 * y al llamarlo en Login.js se especificarían los valores a usar para cada paremotro de la función
 * 
 * FIN DEL EJEMPLO.
*/